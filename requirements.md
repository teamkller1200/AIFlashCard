# AIフラッシュカード生成アプリ（MVP版）要件定義書

---

## 1. システム概要
本システムは、ユーザーが入力したテキスト（学習資料やノート等）をもとに、LLM（大規模言語モデル）を活用して自動的に一問一答形式のフラッシュカード（Q&Aデータ）を生成・表示するWebアプリケーションである。

---

## 2. 目的・コンセプト
* **目的:** 学習資料からの問題作成にかかる時間を短縮し、効率的な暗記学習を支援する。
* **コンセプト:** 機能をMVP（Minimum Viable Product）に絞り込み、シンプルかつ高速に動作するWebアプリを実現する。
* **技術目標:** 最新のOpenAI API（Structured Outputs）とZodによるスキーマ検証を組み合わせ、厳密な型安全性と堅牢なエラーハンドリングを実証する。

---

## 3. システム構成・技術スタック

| 区分 | 採用技術 / サービス | 選定理由 |
| :--- | :--- | :--- |
| **フロントエンド / バックエンド** | Next.js (App Router, React, TypeScript) | フルスタック開発を単一リポジトリで完結させ、サーバーサイド（Route Handlers）で安全にAPI処理を行えるため。 |
| **スタイリング** | Tailwind CSS | UIコンポーネントの迅速な構築とレスポンシブ対応が容易なため。 |
| **外部API (LLM)** | OpenAI API (GPT-4o) | 高精度なテキスト解析と Structured Outputs 機能による確実なJSONデータ取得のため。 |
| **スキーマバリデーション** | Zod | APIレスポンスの検証およびTypeScript型定義の自動生成を行い、型安全性を担保するため。 |
| **ホスティング / デプロイ** | Vercel | Next.jsとの相性が良く、GitHub連携による自動デプロイが容易なため。 |

---

## 4. 機能要件

### 4.1. 画面・UI機能
* **テキスト入力エリア**
  * 学習資料を入力・ペーストできるテキストエリアを提供する。
  * 入力文字数の上限設定（例：最大4,000文字）およびリアルタイム文字数カウンターを配置する。
* **生成実行ボタン**
  * テキスト送信およびAIリクエストをトリガーする。
  * 未入力時や文字数上限超過時はボタンを非活性（Disabled）状態にする。
* **ステータス・フィードバック表示**
  * API通信中は生成処理中を示すローディング表示（スピナーおよびスケルトンUI）を表示する。
  * エラー発生時（ネットワークエラー、文字数オーバー、API制限等）はユーザー向けのフィードバック（トースト通知）を表示する。
* **カード一覧表示エリア**
  * 生成された一問一答（質問と回答）を視覚的に分かりやすいカード形式で一覧表示する。
  * 表裏の切り替え（タップ/クリックで回答表示）や簡易的なめくりUIを提供する。

### 4.2. バックエンド・API連携機能
* **プロンプト処理・JSON出力制御**
  * 入力テキストを受け取り、OpenAI APIへリクエストを送信して質問・回答ペア（デフォルト3件）を生成する。
  * OpenAI APIの `response_format` (Structured Outputs) を指定し、定義済みのZodスキーマに沿ったデータのみを出力させる。
* **型安全性の確保 & バリデーション**
  * APIからのレスポンスをZodでパース・バリデーションし、不正な形式の場合は安全にエラーとして処理する。
* **エラーハンドリング**
  * OpenAI APIのタイムアウト、レートリミット、無効なキーエラー等をキャッチし、適切なHTTPステータスコード（400, 429, 500等）とエラーメッセージを返却する。

---

## 5. 非機能要件
* **操作性（Usability）**
  * 直感的で迷わないUI/UXを設計し、PC・モバイル双方の画面サイズに対応したレスポンシブデザインを適用する。
* **保守性・拡張性（Maintainability）**
  * コンポーネントを適切に分割し、Zodスキーマを中心とした型定義の一元化を図ることで、将来的な機能拡張（DB保存やPDF対応）に耐えうる設計とする。
* **セキュリティ（Security）**
  * APIキー等の機密情報はサーバーサイド（`env.local` / Vercel環境変数）で管理し、クライアント側へ露出させない。
  * クライアントからの過剰なAPI呼び出しを防ぐため、簡易的なレートリミット対策を考慮する。

---

## 6. データ構造定義（APIレスポンス仕様）

### 6.1. Zod スキーマ定義
```typescript
import { z } from 'zod';

export const FlashCardSchema = z.object({
  id: z.number(),
  question: z.string(),
  answer: z.string(),
});

export const FlashCardResponseSchema = z.object({
  cards: z.array(FlashCardSchema),
});

export type FlashCard = z.infer<typeof FlashCardSchema>;
export type FlashCardResponse = z.infer<typeof FlashCardResponseSchema>;
```

### 6.2. JSONレスポンス例
```json
{
  "cards": [
    {
      "id": 1,
      "question": "Webにおける「HTTP」の正式名称は何でしょうか？",
      "answer": "HyperText Transfer Protocol（ハイパーテキスト・トランスファー・プロトコル）"
    },
    {
      "id": 2,
      "question": "REST APIにおけるGETメソッドの主な役割は何ですか？",
      "answer": "サーバーから指定したリソースの情報を取得すること。"
    },
    {
      "id": 3,
      "question": "TypeScriptを使用する主なメリットは何ですか？",
      "answer": "静的型付けにより、コード実行前に型エラーを検知して開発効率と保守性を向上させる点。"
    }
  ]
}
```

---

## 7. 今後の拡張プラン（フェーズ2以降）
* **ファイルアップロード機能:** PDFやテキストファイルの直接アップロードおよびテキスト抽出し、カード生成する機能。
* **認証・データ保存機能:** NextAuth.js / Supabase Auth を導入し、作成したカードをPostgreSQLに保存・管理する機能。
* **学習支援アルゴリズム機能:** 忘却曲線に基づく分散学習（Spaced Repetition）アルゴリズムの実装による復習タイミングの提示。
